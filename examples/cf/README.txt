this example assumes you have consul running
and the example sets the CONSUL_HOSTPORT env var
to the consul host and port value

the excample currently assumes you have run the
docker example, master-replica, from 
the crunchy container suite located here:
crunchy-containers/examples/docker/master-replica
